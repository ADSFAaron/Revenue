# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/

# Web related

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

/android/key.properties
lib/firebase_options.dart
ios/Runner/GoogleService-Info.plist
firebase.json
google-services.json
android/app/google-services.json

# Firebase Hosting deploy cache
.firebase/

# Local-only notes, never published (security findings, private context)
*.local.md

# Agent skills fetched from upstream per skills-lock.json — the lock file is
# committed, the fetched content is not (same split as pubspec.lock/.dart_tool).
.agents/
