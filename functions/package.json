{
  "name": "revenue-functions",
  "description": "WebAuthn relying party and photo menu import for Revenue. Both need a credential the app cannot hold: the service account key for minting custom tokens, and the model API key for reading a menu off a photo.",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "npm run build && node --test \"test/*.test.js\""
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.2.2",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^26.3.0"
  }
}
