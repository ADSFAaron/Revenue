{
  "name": "revenue-functions",
  "description": "WebAuthn relying party for Revenue. Firebase Authentication has no passkey provider, so a verified assertion is turned into a session by minting a custom token here.",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.2.2",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
